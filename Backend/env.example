# 'tech' means client must supply a key for the tech round (BYOK)
REQUIRE_CLIENT_KEYS=tech

# optional (only used if REQUIRE_CLIENT_KEYS is 'none' or if normal round without BYOK)
GEMINI_API_KEY=
GEMINI_TECH_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
